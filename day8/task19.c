#include<stdio.h>
int main()
{
int n,a,x;
printf("Give Input \n");
scanf("%d",&a);
 n = cub(x);
 c= cub(x)
 d= cube(x)
 e=

return 0;
}
void cub()
{
    int x;
    rintf("Enter the value:");
    scanf("%d",&x);
    printf("Value is:%d",x*x*x);
}
int cub()
{
    int x; 
    scanf("%d",&x);
    return x*x*x;
}
void cub(int x)
{
    printf("Value is:%d",x*x*x);
}
int cub(int x)
{
    return x*x*x;
}
